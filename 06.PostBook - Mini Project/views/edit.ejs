<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edit your post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white px-3 py-2">
        <div class="flex justify-between items-center">
            <div>
                <a href="/profile" class="text-green-500">Back</a>
                <h2 class="text-2xl">Hello, <%= user.name %>👋</h2>
            </div>
            <a class="bg-red-500 rounded-md px-3 py-2" href="/logout">Logout</a>
        </div>
        <!-- <h3 class="text-sm">Welcome to your profile</h3> -->
        <form class="mt-5" action="/update/<%= post._id %>" method="post">
            <textarea class="resize-none w-1/3 bg-zinc-700 rounded-md outline-none p-2 border border-zinc-600 tracking-tight block" name="content" placeholder="update your post"><%= post.content %></textarea>
            <input class="bg-green-500 px-4 py-2 text-sm rounded-md mt-2" type="submit" value="Update Post">
        </form>
        
    </div>
</body>
</html>